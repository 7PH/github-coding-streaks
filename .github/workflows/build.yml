name: Build

on:
    push:
        branches:
            - master
    pull_request:
        branches:
            - master

jobs:
    update:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            - run: npm install
            - run: npm run build
